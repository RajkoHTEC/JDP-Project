<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/linearLayout"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:padding="8dp">

    <Button
        android:id="@+id/btnShowToast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Toast"
        android:paddingVertical="8dp"/>
    <Button
        android:id="@+id/btnShowSnackBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Snackbar"
        android:paddingVertical="8dp"/>
    <Button
        android:id="@+id/btnShowAlertDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Alert Dialog"
        android:paddingVertical="8dp"/>
    <Button
        android:id="@+id/btnShowDialogFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Dialog Fragment"
        android:paddingVertical="8dp"/>
    <Button
        android:id="@+id/btnShowDialogFragmentWithList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show dialog fragment with list"
        android:paddingVertical="8dp"/>
    <Button
        android:id="@+id/btnShowBottomSheetDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show option bottom sheet dialog"
        android:paddingVertical="8dp"/>

</LinearLayout>